# Copy this file to staging.pkrvars.hcl and fill in real values.
# staging.pkrvars.hcl is gitignored so it never gets committed.

region        = "us-east-1"
instance_type = "t3.small"
environment   = "staging"
